ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ── Azure MCP (leave blank to use local mock data) ────────────────────────────
# Set these after deploying the Container App to switch to Azure mode.
# Get AZURE_MCP_ENDPOINT from:
#   az containerapp show --name driver-mcp-server --resource-group sasser-chatbot-rg \
#     --query properties.configuration.ingress.fqdn --output tsv
AZURE_MCP_ENDPOINT=
DRIVER_ID=john_doe
